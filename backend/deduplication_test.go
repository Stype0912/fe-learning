package main

import (
	"testing"
)

func TestDeduplication(t *testing.T) {
	t.Log(p.ProbablyPrime(10))
	t.Log(lambda)
	t.Log(b)
	//v := big.NewInt(320282200009128411)
	//v_hat := DeduplicationUser(v)
	//v_hat := new(big.Int).Add(bSum, v)
	//DeduplicationCommittee(v_hat)
}
